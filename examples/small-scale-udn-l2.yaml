# udn-density-pods, layer l2, small scale (AWS).
# Metadata inherited from parents/aws-density-common.yaml; OVN/OVS metrics
# from metrics/ovn-density-metrics.yaml (density=udn, layer=l2 defaults).
parentConfig: parents/aws-density-common.yaml
metricsFile: metrics/ovn-density-metrics.yaml
tests:
  - name: aws-small-scale-udn-density-l2-pods
    metadata:
      masterNodesType: m6a.xlarge
      masterNodesCount: 3
      workerNodesType: m6a.xlarge
      workerNodesCount: 24
      benchmark.keyword: udn-density-pods
    # encrypted: true
    # ipsec: false
