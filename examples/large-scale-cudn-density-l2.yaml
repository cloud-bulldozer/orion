# cudn-density, layer l2, large scale (AWS).
# Run with: --input-vars '{"version":"4.19","density":"cudn"}'
parentConfig: parents/aws-density-common.yaml
metricsFile: metrics/ovn-density-metrics.yaml
tests:
  - name: aws-large-scale-cudn-density-l2-pods
    metadata:
      masterNodesType: m6a.4xlarge
      masterNodesCount: 3
      workerNodesType: m6a.2xlarge
      workerNodesCount: 252
      infraNodesType: r5.8xlarge
      benchmark.keyword: cudn-density
