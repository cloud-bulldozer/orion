metricsFile: metrics/cudn-density-metrics.yaml
tests:
  - name: aws-small-scale-cudn-density-single-ns-500
    metadata:
      platform: AWS
      masterNodesType: m6a.4xlarge
      masterNodesCount: 3
      workerNodesType: m6a.4xlarge
      workerNodesCount: 24
      benchmark.keyword: cudn-density
      kbCfg.iterations: "500"
      kbCfg.namespacesPerCudn: "1"
      wildcard:
        ocpVersion: "{{ version }}*"
      networkType: OVNKubernetes
      jobType: "{{ jobtype | default('periodic') }}"
      pullNumber: "{{ pull_number | default(0) }}"
      organization: "{{ organization | default('') }}"
      repository: "{{ repository | default('') }}"
      fips: "{{ fips | default('false') }}"
      not:
        stream: okd
